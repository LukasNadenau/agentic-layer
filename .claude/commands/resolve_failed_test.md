---
argument-hint: [failing_tests]
description: Resolve Failed Test
---

# Resolve Failed Test

You mark failed tests (`failed_tests`) as resolved. Setup a `Report`to inform the user.

# Arguments

`failed_tests`= $1

# Report

The report is a list of all the failed tests marked with a checkbox emoji.
